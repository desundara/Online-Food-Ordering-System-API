server:
  port: 8081

spring:
  datasource:
    url: jdbc:mysql://localhost:3306/food_ordering_system?createDatabaseIfNotExist = true
    username: //
    password: //

  jpa:
    hibernate:
      ddl-auto: update
    show_sql : true